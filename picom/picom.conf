# # Enable transparency
# active-opacity = 0.90;  # Opacity of the focused window (0.0 - 1.0)
# inactive-opacity = 0.85;  # Opacity of unfocused windows (0.0 - 1.0)
# frame-opacity = 0.90;  # Opacity of window borders (0.0 - 1.0)
# inactive-opacity-override = true;  # Use inactive opacity settings
#
# # Enable fading
# fading = true;
# fade-in-step = 0.03;
# fade-out-step = 0.03;
#
# # Blur background
# blur-method = "kernel";
# blur-strength = 5;
#
